init_config:

instances:
  - host: %%env_REDIS_HOST%%
    port: %%env_REDIS_PORT%%
    username: %%env_REDIS_USERNAME%%
    password: %%env_REDIS_PASSWORD%%
    ssl: true
    tags:
      - service:ticket-managed-redis
      - component:queue-redis
      - managed:true
